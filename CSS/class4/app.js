// test function

// function that will redirect user to a new page

// AI generated code rick roled me
function redirect() {
  window.location.href = "https://www.youtube.com/watch?v=dQw4w9WgXcQ";
}

// function that will change the text of the button


function changeText() {

}

// function that will change the background color of the button

function changeColor() {
}

// function that will validate email address

function validateEmail() {
  // logic here to validate email address
  // if valid, call the redirect function
  // if not valid, call the changeText function
  // if not valid, call the changeColor function
    const input = document.getElementById("email").value;
    if (input === "XXXXXXXXXXXXX") {
        redirect();
    }
    else {
        changeText();
        changeColor();
    }

}

